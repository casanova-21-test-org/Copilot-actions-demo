name: hello-world
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-earliest
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo "Hello, world!"
